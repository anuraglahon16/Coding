#include<iostream>
#include<math.h>
using namespace std;

int main() {
int arm=0,digit,n,num,r;
cin>>n;
num=n;
while(n>0)
{
    digit=n%10;
    n=n/10;
    arm=arm+digit*digit*digit;
}
if(arm==num)
{
    cout<<"true";
}
else
{
    cout<<"false";
}
}
